# Django Framework
Django==4.2.0

# Additional helpful packages
# Pillow is required for image handling in models
Pillow==10.0.0

# For better admin interface (optional but recommended)
django-admin-interface==0.25.0

# For production deployment (optional)
# gunicorn==21.2.0
# psycopg2-binary==2.9.6  # PostgreSQL adapter

# Development tools
# django-debug-toolbar==4.1.0  # For debugging in development
# django-extensions==3.2.3  # Useful management commands

# Notes:
# - Django 4.2.0 is a stable long-term support version
# - Pillow is needed for handling image uploads in models
# - To install: pip install -r requirements.txt
# - To upgrade pip: pip install --upgrade pip
# - To freeze dependencies: pip freeze > requirements.txt
